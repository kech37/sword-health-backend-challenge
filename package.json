{
  "name": "sword-health-backend-challenge",
  "version": "1.0.0",
  "description": "Sword Health Backend Challenge",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "build:clean": "del-cli dist && npm run build",
    "dev": "npm run build:clean && cd dist/ && clear && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.18",
    "@types/node": "^20.7.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "del-cli": "^5.1.0",
    "express": "^4.18.2"
  }
}
